# Arbor Manage Core

Pure TypeScript core for Arbor task management.

## Verification

```powershell
pnpm --filter @arbor/manage-core test
pnpm --filter @arbor/manage-core typecheck
pnpm --filter @arbor/manage-core build
```

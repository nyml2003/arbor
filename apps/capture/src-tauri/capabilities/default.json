{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local Capture windows.",
  "windows": ["settings", "overlay", "toast"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default"
  ]
}

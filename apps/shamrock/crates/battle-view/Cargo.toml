[package]
name = "battle-view"
version = "0.1.0"
edition = "2024"

[dependencies]
battle-core = { path = "../battle-core" }
battle-data = { path = "../battle-data" }

[dev-dependencies]
battle-format = { path = "../battle-format" }

# 设计实现偏差分析

TEP-0001 ~ TEP-0007 定义的四条协议与五层架构，对比 `crates/arbor-tui-*` 的实际代码，逐项列出偏差。

生成时间: 2026-07-05
最后更新: 2026-07-05 (4 项严重 + 4 项中等 + 2 项轻微偏差 + TEP-0007 五层拆分已修复，E2E 扩展到 80 个 runtime 场景，剩余仅轻微偏差)

---

## 一、总览

四条协议的核心机制均已实现：VirtualScreen + diff（渲染协议）、measure/layout 两趟协商（布局协议）、Signal/ReadSignal + DirtyTracker（响应式协议）、WidgetNode + FocusManager + 事件冒泡（组件协议）。

TEP-0007 的五层 crate 边界已落地：

| 层 | crate | 依赖规则 |
|----|-------|----------|
| domain | `arbor-tui-domain` | 只依赖通用 Rust crate，不依赖应用层、适配器或组件实现 |
| application | `arbor-tui-application` | 正式依赖 `domain`，测试中才使用 adapters/widgets |
| adapters | `arbor-tui-adapters` | 依赖 `domain`，实现真实终端、模拟终端和输入源 |
| widgets | `arbor-tui-widgets` | 依赖 `domain`，只提供内置组件和 builder |
| testing | `arbor-tui-testing` | 依赖五层中的其他 crate，提供 mock 输入/输出 E2E driver、ANSI replay terminal 和屏幕断言 |

`arbor-tui-examples` 是演示入口，不属于核心五层。

以下按 TEP 和严重程度列出具体偏差。

---

## 二、严重偏差（设计意图矛盾或功能断裂）

### 2.1 渲染管线每帧无条件全量执行（TEP-0004）✅ 已修复

**TEP 设计**：事件循环末尾统一渲染，只在有脏组件时才执行管线。

**原代码现状**：`event_loop.rs:run_event_loop()` 每轮 while 循环无条件调用 `app.render_widget_tree()`，不检查 `dirty_tracker.is_empty()`。

```rust
// event_loop.rs:66 — 每轮循环都渲染，不管有没有脏组件
while app.is_running() {
    let events = poll_events(input);
    // ... handle events ...
    app.render_widget_tree(root, theme, backend);  // 无条件执行
}
```

`app.rs:render_widget_tree()` 内部在 `frame_seq > 0` 且 `regions.is_empty()` 时才跳过 emit，但仍执行了完整的 measure → layout → render → diff 管线。无脏组件时这 4 步是纯浪费。

**影响**：空闲时 CPU 不接近零——每 100ms（poll_timeout 超时）执行一次完整的布局+渲染管线。违背 TEP-0001 的"空闲 CPU 接近零"硬约束。

**修复方向**：渲染前检查 `dirty_tracker.is_empty()`，空则直接 return false。或者检查事件队列是否有新事件 + 是否有脏组件，两者皆无才 skip。

---

### 2.2 组件 mount 时不自动订阅 Signal（TEP-0004）✅ 已修复

**TEP 设计**：组件 mount → 订阅依赖的状态值。组件 unmount → 退订。Drop 兜底退订。

**原代码现状**：`mount_tree()` 调用每个 widget 的 `on_mount()`，但所有内置组件的 `on_mount()` 都是默认空实现（来自 `#[derive(Default)]` 的 Widget trait）。没有任何代码在 mount 时调用 `signal.subscribe(widget_id)`。

```rust
// widget.rs — Widget trait 默认实现
fn on_mount(&mut self) {}     // 空
fn on_unmount(&mut self) {}   // 空
```

Drop 实现倒是做了退订：
```rust
// widget.rs:279-283
impl Drop for TextWidget {
    fn drop(&mut self) {
        self.text.unsubscribe(self.id);
        self.style.unsubscribe(self.id);
    }
}
```

订阅退订不对称——退订有，订阅没有。目前只能由应用层手动调用 `signal.subscribe(widget_id)`，这不是 TEP 设计的自动生命周期管理。

**影响**：组件持有 ReadSignal 但未订阅，signal.set() 不会标记该组件为脏，状态变更不触发重渲染。受控组件模式不工作。

**修复方向**：在每个内置组件的 `on_mount()` 中，遍历其持有的 ReadSignal 并调用 `subscribe(self.id)`。或者提供 `Widget::subscriptions()` 方法返回信号列表，由 `mount_tree()` 统一订阅。

---

### 2.3 TabsWidget::on_key 破坏响应式链（TEP-0004 / TEP-0005）✅ 已修复

**TEP 设计**：ReadSignal 是只读视图，由外部 Signal 驱动。

**原代码现状**：`widget.rs:367-390` 中 `TabsWidget::on_key()` 直接覆盖 `self.active`：

```rust
// widget.rs:377 — 用常量覆盖 ReadSignal，断开与源 Signal 的连接
Key::ArrowRight | Key::Char('l') => {
    let next = (old + 1) % self.tabs.len().max(1);
    self.active = ReadSignal::constant(next);  // BUG: 断开响应式链
}
```

`self.active` 字段类型是 `ReadSignal<usize>`。当用户按左右箭头切 tab 时，这行代码用一个新的 `ReadSignal::constant()` 替换了原来的 ReadSignal。原来的 Signal 及其订阅关系全部断开。外部通过 `on_switch` 回调通知了变更，但 `self.active` 和源 Signal 之间不再同步。

**影响**：外部通过 `tabs_signal.set(2, &mut dirty)` 切 tab 无效——TabsWidget 持有的已经是另一个 ReadSignal 了。这是响应式数据流断裂。

**修复方向**：TabsWidget 不应直接持有 `active: ReadSignal<usize>` 并尝试修改它。要么：
- 通过 `on_switch` 回调让外部改 Signal（保持 ReadSignal 只读语义）
- 或使用内部 `active_index: usize` + 外部 `active: ReadSignal<usize>` 双字段

---

### 2.4 Tabs::children() 返回空切片（TEP-0005）✅ 已修复

**TEP 设计**：组件树遍历覆盖所有子组件，焦点遍历进入 tab 内容。

**原代码现状**：`focus.rs:232` 中 `WidgetNode::children()` 对 Tabs 变体返回空：

```rust
// focus.rs:232
WidgetNode::Tabs(_w) => &[],  // Tab 内容不可达
```

这意味着 `FocusManager::build_parent_map()` 和 `collect_focusable()` 不会进入 tab 内容区域。Tab 内的 Input/Button/List 永远不会被加入 tab_order，无法通过 Tab 键导航到。

**影响**：Tab 内容区内的可获焦组件不可达。

**修复方向**：`children()` 应返回当前活跃 tab 的内容节点切片。

---

## 三、中等偏差（功能缺失或集成断裂）

### 3.1 SIGWINCH 检测未集成到事件循环（TEP-0003）✅ 已修复

**TEP 设计**：终端尺寸变化 → 全树重排（SIGWINCH → full relayout）。

**原代码现状**：`signal_manager.rs:49-58` 有 `check_resize()` 函数，但 `event_loop.rs:run_event_loop()` 从未调用它。事件循环只做 `poll_events(input)` 然后直接渲染。

```rust
// signal_manager.rs — 已实现但未调用
pub fn check_resize(app: &mut App, backend: &dyn TerminalBackend) -> bool {
    let (cols, rows) = backend.size();
    let (cur_cols, cur_rows) = app.screen_size();
    if cols != cur_cols || rows != cur_rows {
        app.resize(cols, rows);
        true
    } else {
        false
    }
}
```

Windows 上 resize 事件本来就不会通过 stdin 到达（crossterm 的 Event::Resize 只来自 SIGWINCH），但即便在 Unix 上也没有轮询接入。

**影响**：终端窗口缩放后渲染错位或截断，直到下次全量重绘才恢复。

**修复方向**：在 `run_event_loop` 起始或每轮循环中调用 `check_resize()`，检测到变化时 `app.mark_all_dirty()`。

---

### 3.2 缺少端到端测试（TEP-0006）✅ 已修复

**TEP 设计**：三层测试金字塔——domain 纯函数单元测试 → infra 模拟后端集成测试 → 端到端（模拟输入到后端输出断言）。

**原先缺失**：`pipeline_integration.rs` 覆盖了 measure→layout→render→diff→emit 管线，但只用 `ReadSignal::constant`，不涉及 Signal 变更 → DirtyTracker → 渲染的响应式闭环。

**修复**：`arbor-tui-testing/tests` 现在有 80 个 runtime E2E 场景。新增场景按主题拆成：
- `color_e2e.rs`：复杂 light theme 输入、placeholder 清理、dashboard 选中行、RichText 自定义背景、嵌套 input cursor，以及 3 个 ANSI replay 颜色场景。
- `rendering_e2e.rs`：首帧渲染、空闲不重绘、主题背景、Border、Row/Col、RichText、Table、小视口。
- `resize_e2e.rs`：扩大、缩小、连续 resize、clear 输出、焦点、input buffer、Tabs active 状态。
- `scroll_e2e.rs`：Scroll signal、越界滚动、Border 内滚动、透明 Col child、List/Table 选择自动滚动。
- `keyboard_e2e.rs`：Tab/Shift+Tab、输入编辑、CJK、password、submit/change 回调、Ctrl+C、Ctrl+Q、Escape、Button Enter。
- `reactive_e2e.rs`：Text/Button/Scroll/style signal 更新、同值不重绘、多订阅者同步。
- `workflow_e2e.rs`：Tabs 切换、active child 输入、inactive child 不可见、Border+Tabs+Input、dashboard 脚本。

**影响**：端到端覆盖已从 smoke test 扩展到典型用户路径。后续新增 widget、runtime 行为或真实 ANSI 输出行为时，必须优先补 E2E 场景。

**输出层补充**：`arbor-tui-adapters` 增加 crossterm 输出测试。真实 adapter 对每个可见 cell 写入 foreground、background 和 attrs。partial dirty region 不再调用行尾 clear，避免终端默认背景色覆盖未变更区域。`arbor-tui-testing` 增加测试专用 ANSI replay terminal。它不依赖真实终端，但会按 crossterm 输出字节流回放 SGR、cursor move 和 clear 行为。

---

### 3.3 旧主 crate 与适配器边界混杂（TEP-0006 / TEP-0007）✅ 已修复

**TEP 设计**：运行时状态转换属于 application。真实终端、模拟终端和输入源属于 adapters。application 不能直接依赖 crossterm 或具体 adapter。

**代码现状**：旧 `arbor-tui` 主 crate 已改为 `arbor-tui-application`。它的正式依赖只有 `arbor-tui-domain` 和 `anyhow`。`crossterm` 只存在于 `arbor-tui-adapters` 和示例入口。

**修复**：
- 删除旧 `arbor-tui` 主 crate 名，不保留兼容 re-export。
- `arbor-tui-application` 只编排 runtime step、event loop、resize 和 render intent。
- `arbor-tui-adapters` 实现 `TerminalBackend` 和 `InputReader` 端口。
- `arbor-tui-testing` 承接 `TuiTestDriver` 和 `WidgetHarness`。
- `arbor-tui-examples` 显式组合 application、adapters、widgets 和 crossterm。

---

### 3.4 无基准测试验证硬约束（TEP-0001）✅ 已修复

**TEP 设计**：启动 < 50ms。1080p 全屏帧 < 5ms。空闲 CPU 接近零。内存 < 5MB。二进制 ≤ 8MB 静态链接。

**代码现状**：Release profile 配置了体积优化（opt-level="z", lto, strip, panic="abort"）。

**修复**：`app.rs` 新增 `FrameStats` 结构体和 `App::last_frame_stats` 字段。`render_widget_tree()` 在每次渲染时自动记录各阶段微秒级耗时（layout_us / render_us / diff_us / emit_us / total_us）和脏组件/脏区数量。调用方可直接读取 `app.last_frame_stats` 做性能观测和回归检测，无需 `profile` feature 或外部工具。

---

## 四、轻微偏差（细节不一致）

### 4.1 Attrs 比 TEP 描述多 dim 和 reverse（TEP-0002）

**TEP 设计**：Cell 属性为"粗体/斜体/下划线等标记"。

**代码现状**：`Attrs` 包含 5 个 bool：bold, dim, italic, underline, reverse。TEP 只提了粗体/斜体/下划线，实际多了 dim 和 reverse。

**影响**：无。这是增强而非偏差。

---

### 4.2 弹性余数分配策略与 CSS 不一致（TEP-0003）

**TEP 设计**："弹性余数轮询分配"。

**代码现状**：`layout_engine.rs:353-369` 按 flex 降序排列 flex 子项，然后轮询分配余数像素。这不同于 CSS Flexbox 的"按 flex 比例分配"策略——轮询确保了整数像素分配确定性但可能不按比例。

**影响**：低。TEP 明确说这是手写 Flexbox 子集，整数像素分配策略是设计选择。1px 精度在终端网格中不可见。

---

## 五、TEP-0006 状态

TEP-0006 自身状态为 "Draft"，承认 CI、交叉编译、内存压力测试、dogfooding、crates.io 发布全部暂缓。TEP-0007 已把 crate 边界收敛为五层。代码现状如下：

| 项 | TEP 目标 | 现状 |
|----|---------|------|
| CI | 暂缓 | 无 |
| 交叉编译 (4 platforms) | 暂缓 | 未配置 |
| 内存压力测试 | 暂缓 | 无 |
| Dogfooding (netmon) | 暂缓 | 无 |
| crates.io 发布 | 暂缓 | 无 |
| E2E 测试 | domain+infra+集成 | `arbor-tui-testing` 有 80 个 runtime E2E 场景，覆盖模拟输入、状态、渲染、样式、颜色、滚动、缩放、输出和 ANSI replay |

---

## 六、总结

| # | 类别 | 严重 | 涉及 TEP |
|---|------|------|----------|
| 2.1 | 渲染管线无条件全量执行 | **严重（已修复）** | TEP-0004 |
| 2.2 | mount 不自动订阅 Signal | **严重（已修复）** | TEP-0004 |
| 2.3 | TabsWidget 破坏响应式链 | **严重（已修复）** | TEP-0004, TEP-0005 |
| 2.4 | Tabs::children() 返回空切片 | **严重（已修复）** | TEP-0005 |
| 3.1 | SIGWINCH 未集成到事件循环 | 中等（已修复） | TEP-0003 |
| 3.2 | 无 E2E 测试 | 中等（已修复） | TEP-0006 |
| 3.3 | 旧主 crate 与适配器边界混杂 | 中等（已修复） | TEP-0006, TEP-0007 |
| 3.4 | 无 benchmark 验证硬约束 | 中等（已修复） | TEP-0001 |
| 4.1 | Attrs 比 TEP 描述多 dim 和 reverse | 轻微 | TEP-0002 |
| 4.2 | 弹性余数分配策略与 CSS 不一致 | 轻微 | TEP-0003 |

**四条协议的整体评价**：

- **渲染协议（TEP-0002）**：实现度最高，Cell / VirtualScreen / diff / 后端适配器均按设计完成。无显著偏差。
- **布局协议（TEP-0003）**：两趟协商和 Flexbox 子集正确。SIGWINCH 已接入事件循环。剩余差异是弹性余数分配策略与 CSS Flexbox 不完全一致。
- **响应式协议（TEP-0004）**：Signal/ReadSignal + DirtyTracker 已形成闭环。Text/Button/Scroll/style signal 已有 runtime E2E 覆盖。
- **组件协议（TEP-0005）**：WidgetNode 枚举、焦点管理和 Tabs 活跃内容遍历已可工作。Tab/Shift+Tab、Tabs 切换、Button Enter、Input 编辑和组合 dashboard 已有 runtime E2E 覆盖。

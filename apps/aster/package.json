{
  "name": "@arbor/aster",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local DeepSeek-powered agent CLI",
  "bin": {
    "aster": "./dist/cli.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "chat": "node dist/cli.js",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^5.9.3"
  }
}
